// create table car with columns id, brand, model, year, color
CREATE TABLE car (
    id SERIAL PRIMARY KEY,
    brand VARCHAR(255),
    model VARCHAR(255),
    car_year INT,
    color VARCHAR(255)
)
